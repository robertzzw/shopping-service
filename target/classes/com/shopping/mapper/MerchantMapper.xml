<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopping.mapper.MerchantMapper">


    <update id="addMerchantBalance">
        update `merchant` set account_balance = account_balance + #{amount}
        where merchant_id = #{merchantId}
    </update>
    <select id="selectMerchantForUpdate" resultType="com.shopping.entity.Merchant">
        select * from `merchant` where merchant_id = #{merchantId} for update
    </select>
</mapper>